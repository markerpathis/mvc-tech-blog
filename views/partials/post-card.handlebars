<section class="card">
  <a href="/api/posts/{{id}}">
    <header>{{title}} [{{format_date date_created}}]</header>
    <body>
      {{!-- <p>
        {{content}}
      </p> --}}
    </body>
  </a>
</section>