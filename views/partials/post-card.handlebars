<section class="card">
  <a href="/post/{{id}}">
    <header>{{title}} [{{format_date date_created}}]</header>
    <body>
      {{!-- <p>
        {{content}}
      </p> --}}
    </body>
  </a>
</section>