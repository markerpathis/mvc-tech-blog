<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Tech Blog Title...</title>

  </head>
  <body>
    <header class="py-3">
      <h1 class="text-center">THIS IS A TECH BLOG...</h1>
    </header>
    <div class="nav-scroller mb-3">
      <nav class="nav d-flex justify-content-center">
        <div class="">
          <a class="p-2 text-muted" href="/">Homepage</a>
          <a class="p-2 text-muted" href="/dashboard">Dashboard</a>
          {{#if logged_in}}
            <a class="p-2 text-muted" href="" id="logout-link">Logout</a>
          {{else}}
            <a class="p-2 text-muted" href="/login">Login</a>
          {{/if}}
        </div>
      </nav>
    </div>
    <main role="main" class="container">
      <div class="row">
        <div class="col-md-12">
          {{{body}}}
        </div>
      </div>
    </main>

    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>